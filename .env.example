# Copy to .env.local for local development. Never commit secret values.
# Cloud Run receives the two secrets from Secret Manager instead.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=qwen/qwen3.7-flash
CLASS_DEMO_USERNAME=class
CLASS_DEMO_PASSWORD=
